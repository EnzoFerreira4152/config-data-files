server:
  port: ${PORT:0}

spring:
  application:
    name: api-movie
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql:3366/moviedb
    username: 'usr-movie'
    password: 'pwd-movie'
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
  zipkin:
    base-url: http://zipkin:9411
    enabled: true
    sender:
      type: rabbit
  rabbitmq:
    addresses: rabbitmq:5672
    virtual-host: app_host
    username: guest
    password: guest

eureka:
  instance:
    instanceId: ${spring.application.name}:${spring.application.instance-id:${random.value}}